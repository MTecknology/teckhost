# Managed by ansible. Do not edit.
#
# This has to be a limits.d drop-in rather than a line in limits.conf: Debian
# ships 10-coredump-debian.conf setting "* hard core infinity", and limits.d is
# read after limits.conf, so anything set there is overridden. The 90- prefix
# sorts this after the distro's own drop-ins.

# CIS 1.5.1: Restrict core dumps
* hard core 0
